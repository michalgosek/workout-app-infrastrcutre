const ENDPOINTS = {
    ALL_TRAININGS: "http://localhost:8070/api/v1/trainings"
}

const HTTP_METHODS = {
    GET: "GET"
};

const JSON_HEADER = {
    "content-type": "application/json",
};

export {
    ENDPOINTS,
    JSON_HEADER,
    HTTP_METHODS
};